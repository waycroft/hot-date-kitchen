{
  "name": "hot-dates-kitchen-auto-shipping-labels",
  "version": "0.0.0",
  "private": true,
  "scripts": {
	"build": "bun build --entrypoints src/index.js --outdir dist --target=bun",
    "runtime": "bun --version",
    "dev": "bun --watch run src/index.js",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "prettier": "^3.5.3"
  },
  "dependencies": {
    "@sentry/bun": "^9.27.0",
    "easypost": "workspace:*",
    "jsonata": "^2.0.6",
    "luxon": "^3.6.1",
    "puppeteer": "^24.11.1",
    "shopify": "workspace:*"
  }
}
